import React from "react";

const Navbar = () => (
	<div>
		<h1 className="title-container__title">Weather App</h1>
		<h3 className="title-container__subtitle">Check the weather of any location, as well as temperature and daily forecast</h3>
	</div>
);

export default Navbar;